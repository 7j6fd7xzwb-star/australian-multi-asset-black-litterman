# Data directory

The analysis downloads its inputs at runtime from Yahoo Finance, the Reserve Bank of Australia, and the Kenneth R. French Data Library.

Raw and processed data files are intentionally excluded from version control. This avoids redistributing third-party market data and makes the provenance of every clean run explicit. Empty `.gitkeep` files preserve the expected directory structure.
